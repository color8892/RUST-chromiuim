[workspace]
members = ["rust/hot_leaf/http_header_scanner"]
resolver = "2"

[workspace.package]
edition = "2021"
license = "BSD-3-Clause"
version = "0.1.0"

[profile.release]
codegen-units = 1
debug = false
lto = "thin"
opt-level = 3
overflow-checks = false
panic = "abort"
strip = "symbols"
